# Example .doppler.gist configuration file
# Copy this to .doppler.gist for local development
#
# IMPORTANT: The Gist should ONLY contain the Doppler service token (DOPPLER_TOKEN),
# NOT actual secrets. The service token is used to authenticate with Doppler,
# which then provides the actual secrets securely.
#
# Team Gist Configuration (default):
GIST_ID=879ddbec01b9c6ebaa04bc1fd1630ad8
GIST_USER=wistb
GIST_FILE=.doppler.gist

# The Gist file can contain the token in either format:
# 1. Just the token: dp.st.dev.xxxx
# 2. With variable: DOPPLER_TOKEN=dp.st.dev.xxxx
#
# The setup script will automatically detect and extract the token.

