# Doppler Service Token .env option
# Rename this file to .env (without .example) and add token
# This file is gitignored and will be loaded automatically by VS Code Dev Containers
# No other secrets should be necessary here
#
# Steps:
# 1. Get token from team Discord
# 2. Copy: mv .devcontainer/.env.example .devcontainer/.env
# 3. Edit .devcontainer/.env and replace below with token
# 4. Open container - Doppler will be configured automatically, check with `doppler configure`

DOPPLER_TOKEN=dp.st.dev.....
