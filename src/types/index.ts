/**
 * Shared type definitions for Natural-Highs application.
 * Types are exported from this barrel file for easy imports.
 */

// Re-export types from server types if needed
// export type { UserProfile, Event, Survey } from '@/server/types'

export {}
